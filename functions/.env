# Cloudflare Worker fetch proxy (works around disboards' GCP-IP 403).
# Non-secret: the Worker is gated by SCRAPER_PROXY_SECRET (Firebase secret).
# Leave empty to fetch disboards directly.
SCRAPER_PROXY_URL=https://dvc-rofr-proxy.workinghard.workers.dev
